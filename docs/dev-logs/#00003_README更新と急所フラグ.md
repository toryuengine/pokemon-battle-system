## 日時
2026-09-20 11:39

### 目的
- README.mdをPP管理実装後の状態に合わせて更新する
- 「急所（会心の一撃）」について、技ごとに当たりやすさが異なる仕様をどう実装するか方針を決め、該当する技にデータを追加する

### 実施内容
- README.mdの「未実装」セクションから、既に実装済みとなった「PP管理」の項目を削除
- 新たに「既知の不整合」セクションを追加し、「わるあがき」を暫定的にノーマルタイプ技として実装しているため、本来タイプ無効化を受けないはずのゴーストタイプ相手に0倍になってしまう点を明記
- 「急所に当たる技とそうでない技がある」という仕様について、ユーザーから「専用のリスト（ソース）を別途作るか、それとも技クラス単位にデータを持たせるか」を問われた
  - 別リストにすると情報が2箇所（技クラスと外部リスト）に分散し、以前の特性ID重複問題と同じ管理リスクがあると説明
  - 技クラス自身に`high_crit`属性を持たせる方針を提案し、了承を得た
- `move/base_move.py`の`BaseMove.__init__`に`self.high_crit = False`をデフォルトで追加
- 第4世代で公式に「急所ランク+1」を持つ技を洗い出し、該当する10技（シャドークロー・リーフブレード・つじぎり・きりさく・サイコカッター・クロスチョップ・エアカッター・こうげきしれい・はっぱカッター・クロスポイズン、id: 48, 57, 60, 90, 101, 150, 152, 157, 183, 213）の`move_XXX.py`ファイルに`self.high_crit = True`を追記
- ユーザーから「該当する技に対して全て追加してくれますか」と再度確認されたため、`data/move.json`の技名を「チョップ」「はさみ」「ドリル」「カッター」「クロス」「ぎり」等のキーワードで再検索し、見落としが無いか再チェック。結果、追加で該当する技は見つからず、既存の10技で網羅できていることを確認

### 変更ファイル
- `README.md`
- `src/move/base_move.py`
- `src/move/move_048.py`
- `src/move/move_057.py`
- `src/move/move_060.py`
- `src/move/move_090.py`
- `src/move/move_101.py`
- `src/move/move_150.py`
- `src/move/move_152.py`
- `src/move/move_157.py`
- `src/move/move_183.py`
- `src/move/move_213.py`

### 結果
- README.mdは実装状況を正しく反映した状態に更新済み
- `high_crit`属性は10技全てに正しく付与されていることを`create_move`経由の動作確認で確認済み（該当技は`True`、それ以外の例は`False`のまま）
- `main.py`のシミュレーションもエラー無く動作することを確認済み
- ただし`high_crit`は現状「データとして持たせただけ」であり、実際の急所発生判定やダメージ倍率計算（`battlelogic/damage.py`）にはまだ組み込まれていない

### 次のステップ
- `high_crit`フラグを使った実際の急所発生判定（通常より高い確率で急所に当たる）とダメージ倍率計算を`battlelogic/damage.py`に実装する
- README「既知の不整合」（わるあがきのタイプ無効化）の対応は保留中
- #00001・#00002から続く残タスク（能力ランク補正の反映、優先度判定、状態異常の毎ターン処理、天候・設置技・壁・溜め技・反動硬直・複数回攻撃・一撃必殺・変身系・交代拘束系、`Trainer`クラスによる複数体所持・交代）は引き続き未着手
