## 日時
2026-09-23 21:07

### 目的
がむしゃらのHP依存威力実装、シミュレーション速度の見積もり回答、とんぼがえり（攻撃後の自主交代技）の実装

### 実施内容
- ユーザーから「がむしゃらの実装を開始してください」と指示され、前回(#00011)で実装済みの`has_hp_based_power`の仕組みを`move_172.py`にそのまま設定。じたばたと同じ威力テーブルを使うことを確認・実行
- ユーザーから「1時間シミュレーションを回し続けたら何試合できるか」と質問され、実際にベンチマークを実施:
  - 固定組み合わせ(リザードンvsメガニウム)2000試合で0.047秒 → 1時間換算で約1.5億試合
  - ランダム組み合わせ2000試合で0.062秒 → 1時間換算で約1.16億試合
  という結果を回答
- ユーザーから「とんぼがえりをどう解釈しているか」と聞かれ、通常のダメージ技であることに加えて「ダメージを与えた直後に自分から手持ちの別の1体に強制的に交代する」効果であることを説明。手持ちに生存個体がいなければ交代せず攻撃のみで終わることも説明し、承認を得て実装に着手
- `move/base_move.py`に`self_switch`効果を追加し、`battlelogic/battle.py`に`perform_self_switch`（既存の`switch_in`を再利用し、`Trainer.find_next_alive_index`と同じ選び方＝手持ち順で最初に見つかった生存個体に交代）を実装
- `move_073.py`（とんぼがえり）に`effects = [("self_switch",)]`を設定
- 3体パーティでの交代確認、単体パーティ（交代先が無い場合は交代しない）の確認、1500試合規模のランダムテスト（とんぼがえり混在パーティ）をいずれも実施しエラー無しを確認
- README.mdを更新: 未実装リストの「とんぼがえり等、攻撃後に自分から交代する技」の項目を「拘束技（まきつく等）」のみに絞り、「複数体所持・交代について」セクションにとんぼがえり対応済みである旨を追記

### 変更ファイル
- `src/move/move_172.py`（がむしゃら）
- `src/move/base_move.py`
- `src/move/move_073.py`（とんぼがえり）
- `src/battlelogic/battle.py`
- `README.md`

### 結果
- がむしゃら・とんぼがえりとも動作確認済み（がむしゃらはじたばたと同じテーブルで正しく威力が変化、とんぼがえりはダメージ後の強制交代・交代先が無い場合の非交代いずれも想定通り）
- シミュレーション速度は非常に高速（1時間で億単位の試合数が可能）であることを確認・報告済み
- 1500試合規模のランダムテストでエラー無し

### 次のステップ
- README未実装リストに残るのは拘束技（まきつく等、複数ターンの行動制限）のみ
- いたみわけ（Pain Split, id258）の正確な仕様（HPの平均化）についてユーザーへの確認がまだ完了していない（#00011から持ち越し）
- まねっこ・ものまねの交代時復元処理が引き続き未実装
- `move_263.py`（テクスチャー2）が実在の技か不明瞭なまま要確認扱いで残っている
